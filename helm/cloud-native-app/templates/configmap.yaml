apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "cloud-native-app.fullname" . }}-config
  labels:
    {{- include "cloud-native-app.labels" . | nindent 4 }}

data:
  APP_ENV: {{ .Values.app.environment | quote }}
  LOG_LEVEL: {{ .Values.app.logLevel | quote }}